const defaultState = {
  current: -1,
  contexts: []
}

function layersReducer (state = { ...defaultState }, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default layersReducer
